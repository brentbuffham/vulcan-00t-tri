# VULCAN .00t — RESUME POINT (written end of 2026-07-04 evening session)

**Read this FIRST when resuming. Supersedes RESUME-2026-07-05.md** (kept for
history; its "73.6% teacher-forced value decode" and "carry ~606 tokens"
figures are CORRECTED below — do not act on them).

## USER'S DECLARED PATH (follow it)
1. Solve ALL vertices GT-free on intercepts first.
2. Cold-validate the same rules on a second .00t from Downloads.
3. Only then faces/triangles. Rules must be solid ACROSS files — no soft
   scoring, no fingerprints.

## ONE-SENTENCE STATE
The per-token coordinate VALUE rule is solved (R2, 98.55% deterministic,
teacher-forced); GT-free decode is at **24.3%** (`deterministic_v5b.py`, was
2.2% this morning) with emission count nearly exact (2937/2975 on v5-P2);
the remaining wall is ONE event family: implicit-leading-byte value tokens
(~180 sites) whose carry is LOCALLY AMBIGUOUS — needs segment-level
disambiguation, not more local byte rules.

## CORRECTIONS TO 07-05 DOC (verified this session)
1. **"73.6% teacher-forced value decode" was a scoring artifact**: it demanded
   exact-byte match including the last mantissa byte (~5 NANOMETRES) and used
   stale references (registers advanced only on byte-true labels, so GT-rounded
   yid2/lenY/lenZ tokens left prev 1-2 vertices old). Scored on VALUE (1mm tol)
   with true prev1: strict rule = **98.51%**.
2. **Carry is nearly nonexistent** (4 tokens, not ~606). The "carry" family was
   the same stale-reference artifact.
3. **Placement window floats whole**: r=0 always (07-05 fact #1 stands), but
   end ∈ {8: 1600, 7: 2418, 6: 17} — window = [end-nb, end). T1->k0 with
   end=k0+nb covers it. The ~71 residual misses are alt-k0 (T1-rule wrong k0),
   4 prev2-ref, 2 open. No byte-local signal found for end (payload[0]
   ranges clean; minimality-consistency check FAILS as an override — 80%).

## THE VALUE RULE R2 (bake-off winner — use this, don't re-derive)
```
k0 = T1 rule:  0x21-0x3F -> 2;  0x40-0x5F -> 3;  0x20 -> (3 if T2<0x60 else 2)
               no T -> {nb4:3, nb5:3, nb6:2};  clamp k0+nb<=8
splice payload at [k0,k0+nb) onto prev SAME-AXIS register
if result out of band: try prefix-carry c=±1..±4 at byte k0-1,
then placement scan k0=0..8-nb (band-sane, nearest prev)
```
Head-to-head (teacher-forced, plain byte-true, 1mm): R2 98.55% | strict-k0r
98.51% | nearest-prev-no-T1 52.5% (T1 signal is ESSENTIAL) | minimality-
override 80.3% (rejected).

## GT-FREE DECODER STATE
- `python/deterministic_v5.py` — tokenizer (FULL precedence + escape-prefixed
  FULL as own token type 'Fe' + payload truncation) + R2 + 3 phase variants.
  P1 announce = 13.4% exact (was v4 2.2%). Emits 2861 pts vs 2975 GT
  (deficit 114 = slot slips).
- `python/deterministic_v5b.py` — segment phase-voting: split stream at FULLs,
  decode each segment under phase offsets {0,1,2} with STRICT rule (no rescue
  scan — band violations EXPOSE wrong phase), fewest violations wins, FULL
  re-anchor sets ph=band+1. **20.3% exact GT-free.** 392 segments, 71 needed
  a phase switch. (Runs by exec'ing v5's top half — keep them in sync.)
- Break structure (v5-P1): 142 breaks / 141 recoveries, mean good-run ~2.7
  vertices. Errors are LOCAL and self-heal at FULLs. This is event-site
  damage, not permanent desync.

## WHAT'S KNOWN ABOUT THE REMAINING WALL
- Event tokens (refine 19, two1/two2 29+29, split 1, yid2 65) have **NO
  distinctive byte signature** in (T1, p, nb) — checked, distributions overlap
  plain vals. Caveat: these labels come from closing_solve's GT-matcher and
  may partly be ITS fitting artifacts, not real encoder events.
- Adjacent V-tokens advance axis +1 in 98.9% of labeled pairs (gap-1 pairs
  7446/7530); one-FULL-between pairs advance +2 (151 vs 12) — i.e. most FULLs
  DO occupy a slot. Phase-agreement %s vs labels are PARTLY CIRCULAR (labels'
  axis is positional by construction) — trust only end-to-end GT-free recall.
- T1>=0x60 tokens: 31 labels, mostly val role — still unexplained, low volume.

## HAND-TRACE RESULTS (evening part 2 — the break sites are CLASSIFIED)
Tool: session scratchpad `v5b_trace.py` (instrumented v5b: per-break
classification + annotated dumps + mechanism mining). Findings:
1. **Fe framing FIXED (+4 points)**: escape-prefixed FULL =
   `[esc>=0x20][8-byte FULL][1 trailing byte]` = **10 bytes**, not 9.
   Trailing byte varies (07/4f/a7 at sites 29105/29262/66845); after
   consuming it the stream parses cleanly. Matches toy W13 "count byte".
   Patched into deterministic_v5.py (comment at the Fe branch).
2. **Placement table re-mined clean (r=0-only unique matches)** — k0 is
   f(T1,nb) at ~99%: T20 nb4->k0=3(742:1), nb5->3(704:49), nb6->2(31:0);
   T21-3F nb5->2(1424:12), nb6->2(738:0); T40+ nb4->3(273:1), nb5->3(155:4).
   Validates the existing k0_rule. T2 bits do NOT split the residuals.
3. **THE event family (the wall)**: ~180 value tokens where NO splice of any
   ref reproduces fb (label matches EMPTY). Mechanism identified: window
   starts at byte 2 (consistent with T1 k0=2!) but only the TAIL [3,3+nb) is
   emitted; leading byte 2 changes by carry c in ±1..±8 (141/158 solved as
   prev1 k0=3 + carry; rest prev2..8/odd). c correlates with payload[0] hi
   nibble (borrow/carry signature: c=-1 <-> 0xF*, c=+1 <-> 0x0*) — these are
   ADDITIVE big jumps (inter-intercept hops, ~32m/unit on Y, 8m X, 0.125m Z).
4. **REFUTED for the event carry (don't retry any of these)**: c is NOT in
   the token's own T1/T2/p bits; NOT the T2 byte itself (T2==newbyte2 0:149);
   NOT a signed addend (ADD model 179:1 against); NOT in neighbors' T2/p
   (offsets +-1..+-4, MI sweep = noise vs sparsity baseline); NOT the Fe
   trailing byte (0/11 equal, low-bit corr dead); NOT recoverable from the
   next same-axis FULL lookahead (94/149 have none within 80 tokens; hi3
   match 3:52 where present); min-|delta| candidate unions cannibalize plain
   tokens (76-91% vs 98.5%); guarded per-axis thresholds also lose. The
   carry is LOCALLY AMBIGUOUS in every field tested.
5. Break census after Fe fix: 231 boundaries; ~195 value-events, ~32
   tokenizer-label mismatches near Fe sites, 4 phase slips.

## SCOREBOARD (GT-free exact <2mm, intercepts)
v4 2.2% -> v5-P1 12.0/13.4% -> v5b(20.3) -> Fe-fix: **v5-P2 23.6%,
v5-P3 22.2%, v5b 24.3%**. v5-P2/P3 emit 2937-2940 pts of 2975 (slot
accounting nearly exact). P2 (setband: every FULL snaps phase=band+1) now
BEATS P1 announce — announce model is the weaker phase rule after Fe fix.

## LATE-SESSION REFRAME — EVENTS ARE REFERENCE SWITCHES (Mystery C at scale)
The "carry" framing was the wrong lens. Facts (v5b_trace.py, end of file):
- **171/180 events resolve as EXACT c=0 splices against an OLDER same-axis
  register** (full-history search; distance-back 2..16+, beyond the prev2..8
  window searched earlier — which is why they looked like carries).
- Since payload pins bytes [3,8) and the band pins bytes [0,2), an event
  only needs **byte 2** — i.e. WHICH 8m (X) / 32m (Y) / 0.125m (Z) slab.
  Any older register in the right slab works; "the reference" is really
  "the slab". Face-adjacency test came back 82 adjacent : 88 not — 
  INCONCLUSIVE because slab-mates alias (test can't identify the true ref).
- T2-hi5 channel: near-max entropy (4.64/5 bits), zero MI with delta
  sign/exp/mantissa/axis/nb and zero autocorrelation — it is NOT the event
  signal and probably not coordinate data at all (checksum? topology?).
- Event tokens have EXACTLY one T-pair before them (gap=2 bytes, 149/149) —
  no hidden extra records.
- This matches toy Mystery C ("DELTA reference override, prism/L-shape") —
  now confirmed as the production event family.

## NEXT SESSION — DO THIS
1. **Decode the intercepts TOPOLOGY section** (faces/polyline records after
   the coord region — GT-free, it's in the file). Hypothesis: Draco-style
   neighbor prediction — each vertex's delta reference is a topology-adjacent
   ALREADY-DECODED vertex, which explains reference switches at strip/hole
   jumps. If connectivity gives the reference vertex, byte 2 comes with it
   and events are fully deterministic. This UNIFIES the vertex wall with the
   faces work (Mystery A) — decode topology first, then coords.
2. ~~Register-bank local chooser~~ **MEASURED DEAD 2026-07-04 late: 17/180
   correct; events have 8-9+ in-band slab candidates (ambiguity table in
   v5b_trace.py). The slab info is NOT in the coordinate stream. Topology
   (item 1) is the ONLY road — vertices and faces are ONE coupled problem,
   exactly like Draco (connectivity decoded first, coords predicted from
   mesh neighbors).**
3. Merge the winner phase rule (P2 setband vs v5b voting) into ONE parser.
4. Acceptance gate unchanged: GT-free >=95% on intercepts -> SYLVANIA cold
   -> toys regression -> port to js/vulcan00TParser.js.
5. Do NOT revisit: value codec R2 (solved), minimality override (dead),
   nearest-prev-only (dead), ADD model (dead), local min-delta event picking
   (dead), per-token/adjacent carry correlations (dead), T2-hi5 as event
   signal (dead — measured noise), extra-bytes-at-events (dead — gap=2
   always).

## KEY ARTIFACTS
- Analysis scripts this session lived in the session scratchpad ($TMP):
  sel_acc.py, rule_tree2.py (98.51% strict), t_map.py + r1_signal.py (floating
  end), rule_bake.py (R2 bake-off), v5_runs.py (break structure), event_sig.py
  (no event signature). Their FINDINGS are all in this doc; the two decoders
  are committed. Everything else re-derivable from the doc.
- Answer key unchanged: scratchpad commit_labels.json / closing_order.npy /
  intercepts_gt.csv (path inside deterministic_v5.py).
- Viewer js/intercepts_compare.html still embeds an OLD decode — regenerate
  via generate_compare.py if visual check wanted.
