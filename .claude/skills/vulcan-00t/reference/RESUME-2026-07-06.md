# VULCAN .00t — RESUME POINT (written end of 2026-07-04 evening session)

**Read this FIRST when resuming. Supersedes RESUME-2026-07-05.md** (kept for
history; its "73.6% teacher-forced value decode" and "carry ~606 tokens"
figures are CORRECTED below — do not act on them).

## USER'S DECLARED PATH (follow it)
1. Solve ALL vertices GT-free on intercepts first.
2. Cold-validate the same rules on a second .00t from Downloads.
3. Only then faces/triangles. Rules must be solid ACROSS files — no soft
   scoring, no fingerprints.

## ONE-SENTENCE STATE
The per-token coordinate VALUE rule is solved (R2, 98.55% deterministic,
teacher-forced); the GT-free decoder `deterministic_v5.py` went 2.2% -> 13.4%
-> 20.3% (segment phase-voting variant `deterministic_v5b.py`); the remaining
wall is the PHASE/SLOT grammar at event sites — ~140 LOCAL 1-2-vertex breaks,
each self-healing at the next FULL.

## CORRECTIONS TO 07-05 DOC (verified this session)
1. **"73.6% teacher-forced value decode" was a scoring artifact**: it demanded
   exact-byte match including the last mantissa byte (~5 NANOMETRES) and used
   stale references (registers advanced only on byte-true labels, so GT-rounded
   yid2/lenY/lenZ tokens left prev 1-2 vertices old). Scored on VALUE (1mm tol)
   with true prev1: strict rule = **98.51%**.
2. **Carry is nearly nonexistent** (4 tokens, not ~606). The "carry" family was
   the same stale-reference artifact.
3. **Placement window floats whole**: r=0 always (07-05 fact #1 stands), but
   end ∈ {8: 1600, 7: 2418, 6: 17} — window = [end-nb, end). T1->k0 with
   end=k0+nb covers it. The ~71 residual misses are alt-k0 (T1-rule wrong k0),
   4 prev2-ref, 2 open. No byte-local signal found for end (payload[0]
   ranges clean; minimality-consistency check FAILS as an override — 80%).

## THE VALUE RULE R2 (bake-off winner — use this, don't re-derive)
```
k0 = T1 rule:  0x21-0x3F -> 2;  0x40-0x5F -> 3;  0x20 -> (3 if T2<0x60 else 2)
               no T -> {nb4:3, nb5:3, nb6:2};  clamp k0+nb<=8
splice payload at [k0,k0+nb) onto prev SAME-AXIS register
if result out of band: try prefix-carry c=±1..±4 at byte k0-1,
then placement scan k0=0..8-nb (band-sane, nearest prev)
```
Head-to-head (teacher-forced, plain byte-true, 1mm): R2 98.55% | strict-k0r
98.51% | nearest-prev-no-T1 52.5% (T1 signal is ESSENTIAL) | minimality-
override 80.3% (rejected).

## GT-FREE DECODER STATE
- `python/deterministic_v5.py` — tokenizer (FULL precedence + escape-prefixed
  FULL as own token type 'Fe' + payload truncation) + R2 + 3 phase variants.
  P1 announce = 13.4% exact (was v4 2.2%). Emits 2861 pts vs 2975 GT
  (deficit 114 = slot slips).
- `python/deterministic_v5b.py` — segment phase-voting: split stream at FULLs,
  decode each segment under phase offsets {0,1,2} with STRICT rule (no rescue
  scan — band violations EXPOSE wrong phase), fewest violations wins, FULL
  re-anchor sets ph=band+1. **20.3% exact GT-free.** 392 segments, 71 needed
  a phase switch. (Runs by exec'ing v5's top half — keep them in sync.)
- Break structure (v5-P1): 142 breaks / 141 recoveries, mean good-run ~2.7
  vertices. Errors are LOCAL and self-heal at FULLs. This is event-site
  damage, not permanent desync.

## WHAT'S KNOWN ABOUT THE REMAINING WALL
- Event tokens (refine 19, two1/two2 29+29, split 1, yid2 65) have **NO
  distinctive byte signature** in (T1, p, nb) — checked, distributions overlap
  plain vals. Caveat: these labels come from closing_solve's GT-matcher and
  may partly be ITS fitting artifacts, not real encoder events.
- Adjacent V-tokens advance axis +1 in 98.9% of labeled pairs (gap-1 pairs
  7446/7530); one-FULL-between pairs advance +2 (151 vs 12) — i.e. most FULLs
  DO occupy a slot. Phase-agreement %s vs labels are PARTLY CIRCULAR (labels'
  axis is positional by construction) — trust only end-to-end GT-free recall.
- T1>=0x60 tokens: 31 labels, mostly val role — still unexplained, low volume.

## NEXT SESSION — DO THIS
1. Diagnose v5b's remaining breaks the hand-trace way: dump each break site
   (emitted-point index -> token offset), classify what the encoder did there
   (slot skip? double-token coord? unannounced FULL in slot? Fe semantics?).
   They are LOCAL — each fix is worth ~2 vertices x its frequency.
2. Merge segment phase-voting INTO deterministic_v5.py (single sole parser),
   re-race announce-vs-voting hybrids end-to-end.
3. Acceptance gate unchanged: GT-free >=95% on intercepts -> then SYLVANIA
   cold -> toys regression -> port to js/vulcan00TParser.js.
4. Do NOT revisit: value codec (solved, R2), minimality override (dead),
   nearest-prev-only (dead), event byte-signature in (T1,p,nb) (checked, none).

## KEY ARTIFACTS
- Analysis scripts this session lived in the session scratchpad ($TMP):
  sel_acc.py, rule_tree2.py (98.51% strict), t_map.py + r1_signal.py (floating
  end), rule_bake.py (R2 bake-off), v5_runs.py (break structure), event_sig.py
  (no event signature). Their FINDINGS are all in this doc; the two decoders
  are committed. Everything else re-derivable from the doc.
- Answer key unchanged: scratchpad commit_labels.json / closing_order.npy /
  intercepts_gt.csv (path inside deterministic_v5.py).
- Viewer js/intercepts_compare.html still embeds an OLD decode — regenerate
  via generate_compare.py if visual check wanted.
