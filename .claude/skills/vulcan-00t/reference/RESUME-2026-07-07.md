# VULCAN .00t — RESUME POINT (written end of 2026-07-05 session)

**Read this FIRST when resuming. Supersedes RESUME-2026-07-06.md.**
This session executed the 07-06 doc's item 1: decode the intercepts TOPOLOGY
section. Framing + reference encoding are now SOLVED; op semantics is the
remaining wall. Coord-side state unchanged (R2 solved, GT-free 24.3%, ~180
event sites = reference switches).

## ONE-SENTENCE STATE
The intercepts topology section [79563..141701] tokenizes cleanly into 5580
one-face-per-group records whose vertex references are delta-spliced absolute
indices into the coord-emission table (the SAME low-byte-splice trick as the
coordinate codec); what remains is the op state machine that supplies each
face's other two vertices.

## TOPOLOGY FINDINGS (all new this session)
1. **Section framing.** face_start found via E0 03 clustering (<90 bytes for
   4 occurrences). Exactly ONE stray `e0 03` inside the coord region → NO
   coord/topo interleaving; all coords precede all topology. One full W1
   marker `e0 03 14 20 00 40 17` at 122263 = sub-section boundary (matches
   toy sphere sub-sections).
2. **Group grammar** (parser: `python/topo_intercepts_v2.py`):
   - delimiter = tag pair with lead 0xE0/0xE1. Census: e0:03 ×4937 (normal
     face group), e0:04 ×225, e0:02 ×216, e0:ff ×79, others ~100.
   - body := stray skips (0x08-0x1f) · ( op | value )*
   - op := [lead 0x20-0x7f][arg]; **arg low-2-bits == 11 at 92%** — the arg
     carries a 6-bit payload in bits[7:2]. Toy-cube's unsolved lo_nibs
     (7,B,B,3,B,7,7,3,3,F,3,B) use the SAME {3,7,B,F} alphabet → this is
     Mystery A's channel, now visible at scale.
   - `00` before a tag-class byte 0x20-0x7f = SKIP (toy TEST-010 confirmed at
     production; the old "group flag 00 40" reading was wrong — it's skip +
     op `40:17`, the canonical toy first-op).
   - value := count byte 0x01-0x06 → nb=b+1 payload bytes BE; `00`+non-tag
     byte → 1-byte value.
3. **Vertex references SOLVED.** Explicit values are indices into the
   coord-emission vertex table (0..2974): 2-byte = absolute (uniform over
   full range, only 18/1535 outliers = misframes); **1-byte = low-byte
   SPLICE onto the previous reference** (97% in-range under splice, strong
   locality). Delta census between consecutive refs: 0 ×600 (fan pivot),
   ±1..3 (rail steps), −95..−140 cluster (cross-rail hop = COLUMN SIZES →
   ~25 columns of ~100-140 verts, matching production col-major layout).
4. **One group ≈ one face.** Groups with explicit ref: 2694+ ≈ GT reuse-face
   count 2791; groups without: 2710 ≈ GT C-face count 2894 (gap ≈ known
   misframe rate). First-op census: 60:17 ×1756 (1-byte-ref groups),
   40:17 ×628, 42:xx family ×~1400 (2-byte-ref groups), 43:xx ×~500.
   Finalizers 40:1b ×1732 / 20:1b ×1048 (toy 40:1B finalizer confirmed).
5. **DXF entity order ≠ internal face order** (kind sequences don't align at
   the head; DXF starts a C-run, stream starts explicit-heavy). GT face key:
   `ac-653-221-inteceptsDXF.dxf` → 5724 triangles, all corners exact-match
   the 2975 GT verts (extractor: `python/intercepts_dxf_faces.py`,
   faces_gt.npy in session scratchpad). GT mesh: 2894 C-faces (1 new vert),
   2791 reuse, 36 2-new, 3 restarts. 2.5D seam sheet (z 653-658), NOT
   drillhole tubes.
6. **Dead/negative results this session (don't retry):**
   - explicit refs are NOT GT-row ids, NOT (2*id+flag), NOT introduction-
     order ids (id 1452 referenced at group 0 — all verts pre-exist topo).
   - 42:xx arg6 does NOT deterministically encode the ref delta (1330 pairs,
     6% determinism).
   - closing_order.npy as slot map: tokenpos//3 slot mapping gives only 5%
     edge-hit rate (vs 2% random) — matcher damage makes it useless for
     slot-exact work. commit_labels.json 'matches' field is NOT a GT-row map.

## THE REMAINING TOPOLOGY WALL
Op semantics: which two context vertices (gate/rails state) combine with the
group's ref (explicit or implicit-next) to form each face, and how ops
(60:17 / 40:17 / 42:xx+arg6 / 20:03 / 20:ab / fins 40:1b/20:1b / delim
variants e0:02/e0:04) drive the state machine. Attack routes:
1. Bootstrap group↔face alignment WITHOUT π: the ref stream's rail/fan
   structure (delta 0 runs = fans; ±1 runs = strip zips) must match the GT
   mesh's fan/strip structure as an unlabeled graph — align rails to GT
   strip rails, derive π locally, then read op semantics off aligned groups.
2. Exact π via joint solve: teacher-force coords (R2, 98.55%) WITH topo refs
   as extra constraints (referenced-together slots must be GT-mesh
   neighbors) — resolves both the coord event slabs and π in one pass.
3. Then: op semantics fall out from ~5500 aligned (group, face) pairs.

## WHY THIS UNBLOCKS THE COORD WALL (task: Draco-style unification)
The ~180 coord event sites have 8-9 in-band slab candidates (measured dead
locally). Topology refs give mesh edges GT-free; the true slab is the one
placing the vertex near its mesh neighbors. Decode order: topo refs → edge
constraints → event slab disambiguation → GT-free vertex decode >95%.

## ADDENDUM (later same session) — SPARSE-REF GUIDANCE MEASURED DEAD
Attempted vertex-solve via topology adjacency WITHOUT op semantics
(`python/deterministic_v6_topo.py`): v5-P2 core + register history + neighbor
override from the raw ref stream. Results (GT scoring only):
- **Refs = emission slots CONFIRMED**: same-group ref pairs that decode
  exactly sit at 2.65m (grid pitch) — true mesh edges at mesh-edge scale.
- **But the explicit-pair edge set is too sparse/dirty to guide**: only 104
  same-group pairs (15 are rail-steps Δslot=1, ~25 junk from misframes, ~40
  genuine cross-column hops). Cross-GROUP consecutive refs are NOT mesh
  edges (median 35-95m — strips interleave). All guidance variants HURT:
  naive (2904 overrides, 23.6%->5.4%), conservative X+Y (130 ov, ->21.6%),
  Y-only THR=12 (62 ov, ->22.4%, 0 gained / 18 lost).
- **Slab discriminability by proximity**: Y only (32m slab vs 9m max edge).
  X impossible (8m slab vs 8.35m max edge Δx), Z impossible (0.125m slab vs
  0.6m p90 edge Δz). Any adjacency-based slab fix must lean on Y and use
  the DENSE mesh, then propagate X/Z through the register-history splice
  candidates (Y picks the register; X/Z follow from the same reference).
- Decoder slot-counter drift (2937 emitted vs 2975) also corrupts sparse
  lookups; dense edges would allow consistency voting instead.
**CONCLUSION: op state machine (full face decode) is the gating problem for
the vertex solve — no shortcut. Attack per the routes above.**

## ADDENDUM 2 — SWEEP-MOTIF STRUCTURE FOUND (the op-machine scaffold)
Rail clustering of the ref stream (scratchpad `rails.py`, JUMP=6, retire=80):
~25 long-lived rails advancing ±1 per touch; rail visits cycle in FAMILIES
with period ~27 groups. From group ~97 a strict 2-group motif dominates:
    [e003] R@ref   42:xx 20:03 20:xx     <- reuse group (explicit ref)
    [e003]         60:17 22:xx 40:1b     <- idless group (new vertex)
KEY FACTS:
- Op bytes are IDENTICAL across different rails at the same sweep phase
  (groups 101/103/105 = rails R3/R9/R13, ops byte-equal 42:07 20:03 20:7b;
  same triple returns at 128/130/132 one slot lower, byte-equal again).
- Rail triples repeat: (R3,R9,R13)@101-105 -> @128-132 -> @156-158;
  (R8,R22,*)@113-117 -> @142-146; (R2,R15/R9,R14)@120-124 -> @148-152.
- First-op arg 6-bit channel steps by +6 between phases (07->1f->37->4f->67
  = +0x18 in byte); constant within a phase. It is a SWEEP-PHASE COUNTER,
  not rail data → decodable state, not per-face entropy.
- Delimiter variants (e0:07/08/0a/0b, e1:xx) appear as CONTINUATION records
  after groups whose ops overflow (2-record groups), and at strip starts.
- Stream head (groups 0-96) is the irregular boot region (strip starts);
  regular regime after. Fit the machine on the regular regime FIRST, then
  peel the boot region.
NEXT CONCRETE STEP: build the sweep-machine fit over groups 97-1000: state =
(per-family rail positions, phase counter); predict each group's face as quad
(old_i, old_i+1, new_j, new_j+1) triangulated; validate GT-free by slot
coverage + edge-length via anchors; then GT-score the face set.

## ADDENDUM 3 — GEOMETRIC EDGE-BOOTSTRAP MEASURED DEAD (3 variants)
Tried to shortcut the op machine by fitting cross-column edges geometrically
from the baseline decode, then correcting Y. ALL variants hurt the score:
1. XZ-nearest fit (scratchpad ypost.py): median pair dXZ 2.43m LOOKS like
   grid pitch but is a PROJECTION ARTIFACT — on a 2.5D seam sheet, XZ-close
   points can be far apart in Y (northing). Y post-pass: 23.6% -> 17.1%.
2. Per-rail offset consensus: only 60% support; offsets drift with column
   length; XZ-only candidates poison the mode.
3. Implicit-rail sequentiality via XZ-nearest: 23% — same projection artifact.
LESSON (do not retry): edges CANNOT be fitted from the decoded points while
Y is broken — any projection that excludes Y admits false neighbors along
strike, and 3D fits inherit the Y slab errors. The edge set must come from
the OP MACHINE (deterministic rails), not from geometry. Geometry is only
the final validator.
Baseline P and artifacts: scratchpad P_base.npy, pair_fits.npy, ypost.py.
2019 clean motif pairs (1 ref + idless follower, e003) stand ready as the
machine-fit substrate.

## ADDENDUM 4 — GLOBAL TRAVERSAL SHAPE
Median explicit ref vs group index shows TWO descending passes over slot
space (groups 0-2400: 1155 -> 399; jump to ~2315 at 2800; then 2199 -> 1540
to the end; W1 sub-section marker sits at group 3880 of 5580). The topology
traverses the mesh in two sweeps — consistent with two sheets
(hanging-wall / footwall) of the intercept model. No frontier model fits
globally (refs span 0..2950 in every window) — introduction-order remains
dead; the machine is rails + sweeps, not a frontier.

## THE WORKING MODEL TO IMPLEMENT NEXT (synthesis of all addenda)
Evidence says the regular-regime ops carry NO per-face choice: op bytes are
byte-identical across rails at the same phase and their 6-bit args tick a
+6 phase counter (Addendum 2). Therefore:
1. **Strip-start records** (boot region groups 0-96, non-e003 delims, and
   irregular mid-stream groups) INITIALIZE strips: explicit refs pin BOTH
   rails (boot shows 4 rails pinned explicitly: 1452 desc + 1383 asc via
   2-byte; 1453 asc + 1508 desc via 1-byte splice; pair sums 2961/2835 =
   serpentine column meets).
2. **Motif pairs** (2019 of them: [e003] ref-group + [e003] idless group)
   emit ONE QUAD each, fully determined by state: quad(r_k, r_k±1, n_k,
   n_k±1) with the implicit rail n advancing deterministically per family
   cycle (period ~27 groups, families of ~3 rails).
3. Unknowns are ONLY per-strip initial conditions + the family scheduler.
   Fit on groups 97-1000 by exact combinatorics (rail bookkeeping), NOT
   geometry. Validate: quad count -> 2862; slot coverage; THEN anchor-edge
   lengths; THEN dense-edge Y fix; THEN GT score.
DECODER SKELETON: strip table {old_rail(pos,dir), new_rail(pos,dir)};
scheduler cycles strips; e003+ref = reuse step (update old rail from ref,
emit quad); e003 idless = new step (advance new rail, complete quad);
e0:02/04 = strip end variants; e0:07/08/0a/0b + e1:xx = strip start /
continuation records; 40:1b/20:1b finalizers; W1 marker at group 3880 =
sheet boundary (two sweeps, Addendum 4).

## KEY ARTIFACTS
- `python/topo_intercepts_v2.py` — corrected tokenizer + census (committed).
- `python/intercepts_dxf_faces.py` — DXF 3DFACE → GT-index triples (committed).
- Session scratchpad (50aab51b): faces_gt.npy, topo_vals.npy, groups_v2.pkl,
  topo_groups.py/topo_census.py (superseded by v2), slot2row.npy (partial,
  2436/2975 — do not trust for slot-exact work).
- GT: intercepts_gt.csv (old scratchpad 6045a728), DXF in Downloads.
- Coord decoders unchanged: deterministic_v5.py / v5b.

## ADDENDUM 5 (opus session, 2026-07-07) — STRIP-TABLE OP-MACHINE: RAILS ARE
## REAL, but new-vertex allocation is the true wall (not the scheduler)
Implemented the strip-table decoder (`python/topo_strip_decoder.py`) and ran the
full validation ladder. Numbers, no padding:

**WIN — the rail/strip model is STRUCTURALLY CORRECT (decisive, GT-free edges).**
Rail-consecutive ref pairs (from proximity clustering, JUMP=6) are REAL mesh
edges: anchor-edge lengths (P_base + GT anchors, scoring only) = **median 4.04m,
75.9% in the 2-9m grid-pitch band, vs random anchor pairs 135m / 0.7%.** Rails
= mesh columns/strips, confirmed. This is the first GT-free-derived edge set that
proves out geometrically.

**FAIL 1 — bookkeeping (rail identity) cannot be maintained by proximity.**
Strip table over the regular region (groups 97-2400, 1281 refs):
TOL=3/RET=250 → 84.1% matched, 15.9% new-rail "violations"; best tuning
(TOL=4/RET=500) → 86.3% but 55 active rails at end (2x the expected ~25 → rail
identity SPLITS). The "zero violations" target was NOT met. Cause: ~25 concurrent
rails packed into slot band ~700-1600 (spacing ~35) with per-touch steps
d∈{0,±1,±2} (90% within ±2, d=0 most common = fan pivots) means a rail returning
after a long scheduler gap lands on a NEIGHBOR rail's drifted slot. The SCHEDULER
is needed to disambiguate, but the rail-id schedule is **NOT cleanly periodic**
(autocorrelation max 0.15 at lag ~18/37; families of 3 like (r3,r9,r13) recur but
the global cycle drifts and new rails keep spawning). 3 structural variants
(proximity / tuned-retire / schedule-period) all failed the strict check → per
method discipline, stopped fitting the scheduler.

**FAIL 2 — quad count & coverage NOT achieved; the real blocker is new-vertex
(idless-group) SLOT ALLOCATION, not the scheduler.** Only **1657 / 2975 slots
(55.7%) ever appear as an explicit ref.** The other **1325 slots never appear in
the ref stream** — they are the vertices introduced by the 2779 idless C-face
groups, whose emission-slot index is supplied by the op machine, NOT encoded.
Without that allocation rule I cannot place the idless new vertex, so no quad
(quad = r_k,r_k±1,n_k,n_k±1 needs n) can be emitted. Quad count: 0 produced.
Coverage: 55.7% (the reuse half only). **This — not the family scheduler — is the
gating unknown. The ADDENDUM-2 "n advances deterministically per family" premise
is unverifiable until we know how idless groups pick their slot.**

**FAIL 3 — rail-edge Y-slab guide still HURTS the coord decode (23.6% → 19.2%
Y-only, → 12.0% all-axis).** Cleaner edge set than the Addendum-3 sparse refs
(75.9% real vs sparse/dirty) but SAME outcome. Root cause is NOT edge quality:
it's (a) the decoder's slot-counter drift (2937 emitted vs 2975 ref-slots →
nbr[slot] misaligns with the decode's running index for later slots) and (b) the
override mechanism itself — the "splice onto an older same-axis register to match
neighbor-median Y (±16m TOL)" search finds spurious slab matches and overwrites
correct values (439 Y-overrides, net −130 exact). Adjacency guidance via register
re-splice is a dead mechanism regardless of edge quality; confirms Addendum 3.

**WHAT THE NEXT SESSION SHOULD DO (concrete):**
1. Crack the **idless-group new-vertex slot allocation** — the 1325 never-referenced
   slots. NOTE: global increasing-order allocation is ALREADY REFUTED this session
   (assign next missing slot per idless group in group order → only 1.7% land at
   grid-pitch to a nearby ref, median 109m; `scratchpad/explore9.py`). Allocation
   is COLUMN-LOCAL, not a global counter — the new vertex must be near the idless
   group's active rail. Next: try per-rail new-slot counters (each rail carries its
   own advancing new-vertex pointer; the idless group extends the CURRENT strip's
   new rail). Validate by anchor-edge to the paired reuse ref.
2. Only THEN emit quads and re-check count(≈2862)/coverage/anchor-rungs.
3. Fix the slot↔decode-index drift BEFORE re-attempting any coord guidance: align
   ref-slot to the coord decoder's emission index (find where the decoder drops/
   dups the 38 missing emissions), then the rail edges (proven 75.9% real) can be
   applied by DIRECT SUBSTITUTION of the correct-slab register per neighbor vote —
   NOT by the re-splice search, which is dead.
4. Do NOT re-fit the family scheduler by geometry or by rail-id periodicity — both
   measured dead. Attack allocation-order (route 1) first.

Artifacts: `python/topo_strip_decoder.py` (committed) runs the whole ladder.
Scratchpad explore1-8.py (probes), rails.pkl unchanged.

## USER PATH (unchanged)
All vertices GT-free on intercepts → cold-validate SYLVANIA/OB34 → toys
regression → faces → port to js/vulcan00TParser.js.
