# RESUME 2026-07-08 — ALLOCATION RULE CRACKED: serpentine-mirror per-strip counters

**Read RESUME-2026-07-07.md first for the topology grammar, ref encoding, rails,
and the strip-table model.** This doc records the Fable detective session that
cracked the idless-group new-vertex allocation mechanism (the "true wall" from
ADDENDUM 5) and defines the next steps.

## THE FINDING (verified ~90% against GT edges)

**The idless (C-face) group's new vertex is the mirror of its paired ref on the
adjacent serpentine leg: `n = S − r`, with S locally constant per strip.**
The true mechanism is a **per-strip counter**: `n(t+1) = n(t) − d` (d = ±1,
opposite to the ref rail's direction), independent of r; "constant sum" is the
shadow this casts when refs step uniformly, and S drifts ±1 when refs skip.

Evidence chain (scripts in session scratchpad `50aab51b.../scratchpad/alloc*.py`,
key ones copied to `python/`):

1. **Slot-offset census of TRUE GT edges** (alloc4/alloc6): with the slot↔GT map,
   40–44% of mesh edges have |slot offset| = 1 → coord emission order is itself
   a serpentine mesh walk. Cross-column offsets are 20–150 and VARIABLE — there
   is no constant column width (kills every constant-offset model permanently).
2. **Constant-sum candidate threads** (alloc6): rail 1163↓ has cross-candidates
   1139,1140 (sums 2302, 2302); rail 1554↑ has 1571,1570,1569 (sums 3125×3).
3. **Per-rail S fit scores 89.6%** (alloc7): fit S per rail by candidate vote,
   predict n = S−r, test (gt(r),gt(n)) ∈ GT edges: 558/623 strict hits vs 0.7%
   random. Miss decomposition (alloc8): 22/65 misses are ±1..3 counter slips
   (→ ~93% for the counter law), 43 are rail-assignment noise.
4. **Round-robin structure**: refs at consecutive idless groups cycle through
   ~13 concurrent rails, and the rails come in mirror PAIRS around fold centers
   (1163↓/1167↑, 1085↓/1088↑, 1237↓/1243↑, 1375↓/1387↑, 1029 = fan pivot).
   This is what the sweep-motif "op channel ticks +6 per phase" was echoing:
   channel = position in the round-robin cycle.

## WHAT IS STILL OPEN (the residual wall — much smaller)

**Per-strip initialization: n0 (the fold slot) and d.** Everything else about
allocation is now mechanical. n0 is NOT: a global frontier (non-monotone in gi),
a fixed offset from the strip's first ref, or announced as a plain 2-byte value
we've spotted. First-op census hints: normal idless groups open `60:17`; a
disproportionate share of strip-start/STAY events open `40:17` (0x40 = the
coord grammar's "announce" class) — but event labels are too noisy at current
answer-key density to read the rule (see NEXT).

## DEAD THIS SESSION (do not retry)

- **Rigid global allocation counter**, both directions, every start offset
  (alloc2/alloc5): best 9.0% (c0≈1532, step −1) — that residue is the local law
  showing through drift, not a fit. Ascending ≤5%. (Also: descending-from-2974
  "Spirale-Reversi introduction order" is wrong — coords are fully decoded
  before topology; there is no not-yet-introduced constraint.)
- **Ref-space concurrent constant-sum pairs ≠ mirror sums** (alloc8): those are
  two different strips in the round-robin. Score 0.3%. Mirror S never coexists
  as two explicit ref rails at the same time.
- **Naive rail-handoff S recovery** (alloc12): earlier-rail × later-rail set
  intersection under endpoint-sum S candidates picks wrong partners: 0/62 match
  GT-fitted S, 0/291 edge score. Needs the real strip machine (or denser map),
  not greedy set matching.
- **XZ-proximity diagnostics** (alloc3): still an artifact factory (ADDENDUM 3
  stands); produced junk threads, repeated false neighbors.

## TOOLING WINS

- **Answer-key map expanded 693 → 970 slots** (alloc6): unique-both-ways EXACT
  (X,Z) matching (tol 2mm) — Y is the broken axis, so drop it from matching.
  Saved: `thread.pkl` = (slot2gt, gt2slot, motif-pair rows) in scratchpad.
- **Chained quad reconstruction** (alloc11): candidate n must be GT-neighbor of
  BOTH r and the strip's previous allocation. Works, but 1560/2498 sites are
  unmapped → chains break; op-event labels stay noisy. Map density is THE
  binding constraint now.
- **Giant-ref groups are tokenizer MISPARSES** (g206:117 refs, g1191/g3201:58,
  g2204/g5183:57, g4192:62 — runs of 0/256/1024): treat as unparsed blobs, do
  not feed to rail logic.

## NEXT STEP 1 (OPUS-EXECUTABLE SPEC): densify the answer key

Goal: grow slot↔GT map from 970 toward >2500 so INIT sites become exact.
Algorithm (pure propagation, GT used as the answer key only — this is map
construction for rule-derivation, per established practice):
1. Start from the 970 XZ-exact anchors (`thread.pkl`).
2. Iterate: for each UNMAPPED slot s, collect constraint set = mapped slots in
   {s−1, s+1} (emission adjacency, 40% of GT edges) plus any mapped slot m whose
   fitted-S mirror predicts s (m with rail S: S−m == s). Candidate GT rows =
   intersection of GT-neighbor sets of the constraints' GT rows. Accept iff the
   intersection is a SINGLE row AND that row is unclaimed. Repeat to fixpoint.
3. Report growth curve + collision count (collisions >2% ⇒ stop, report).
4. Rerun alloc11 chain reconstruction with the dense map; emit
   `strip_table.npy`: per strip = (gi_start, gi_end, rail_id, n0, d, len,
   resolved fraction). Also rerun alloc7 S-fit → expect >95% strict hits.
Deliverables: updated thread.pkl, strip_table.npy, short report of INIT list.
STOP and write a failure report if: growth stalls <1500, or S-fit degrades.

## NEXT STEP 2 (FABLE): read the n0/d encoding at exact INIT sites

With exact INITs from step 1: dump raw bytes of the 3 records around each strip
start (including non-e003 delims e0:02/04/07/08, e1:xx, and the 40:17-class
first ops); test: (a) op-arg 6-bit channels or value fields encode n0 or
n0−last_ref; (b) n0 = derived state (fold = boundary of the tiling of already-
allocated blocks — check allocation blocks tile slot space and each n0 = an
end of the previous strip's block ± 1); (c) d flips per column by construction
(serpentine) — verify d = −sign(ref-rail direction) on the strip table.

## THEN: the GT-free decoder + Y fix

Round-robin strip machine: active strip list, per-strip (ref-rail state,
counter n, d); e003 ref group advances ref rail; e003 idless group allocates
n, n−=d; strip start/end records push/pop strips (delim variants e0:02/04 and
e1:xx families to be pinned in step 2). Then Y-disambiguation by direct
register substitution per neighbor vote using edges {(r, S−r)} ∪ {(s, s±1)} —
NOT re-splice search (dead), NOT geometric edge fitting (dead). Validation
ladder: quad count ≈2862 → slot coverage = 2975 exactly once → anchor-edge
2–9m band → Y-slab fix rate → GT exact score (target ≥95%, from 23.6%).
