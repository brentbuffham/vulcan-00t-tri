# RESUME 2026-07-09 — Opus session: pipeline REBUILT from raw; Step-1 densify STALLS (spec halt), reframes to Step 2

**Read RESUME-2026-07-08.md first** (serpentine-mirror allocation, `n=S−r`, the
INIT wall). This is the Opus execution of that doc's NEXT STEP 1 (densify the
answer key). Result: **the propagation densifier is SOUND but STARVED — it
stalls at 1113/2975 (37%), below the doc's own `<1500` halt threshold.** The
stall is fundamental and precisely diagnosed. Hands off to Fable for Step 2,
which — key reframe — does **not** need a dense map.

## DURABILITY FIX (do this understanding first)
All four intermediate artifacts from the Fable session (`thread.pkl`,
`P_base.npy`, `rails.pkl`, `faces_gt.npy`, `intercepts_gt.csv`) had died with
their scratchpads. They are now **regenerated deterministically from raw** by two
committed scripts (run from `python/` cwd, ~5s total):

    python python/rebuild_gt.py         # DXF -> intercepts_gt.csv + faces_gt.npy
    python python/rebuild_pipeline.py   # .00t -> rails.pkl + P_base.npy (+ checks)
    python python/alloc6.py             # -> thread.pkl (970-anchor answer key)

Raw inputs (the only things that must exist):
`C:/Users/brent/Downloads/ac-653-221-intecepts.00t` and
`...-inteceptsDXF.dxf`. **No more scratchpath dependencies.** Every alloc*.py now
reads local `intercepts_gt.csv`. `thread_970.pkl` = pristine 970-anchor backup.

**Reproduction confirmed faithful** (all numbers match RESUME-07-08 exactly):
GT = 2975 verts / 5724 tris; topology groups 5580; rails 740; P_base 2937 pts;
anchor slots 693 (23.6%); XZ-exact unique-both-ways map 970; serpentine-mirror
S-fit 558/623 = **89.6%**. The rebuild is trustworthy.

## WHAT WAS EXECUTED (NEXT STEP 1) — `python/densify.py`
Pure propagation to fixpoint, GT used only as the answer key (no invented values;
every accepted slot forced by a SINGLE GT row). Constraints per unmapped slot s:
(A) mapped emission-neighbors {s−1,s+1}; (B) fitted-S mirror: mapped ref r with
S−r==s. Candidate rows = INTERSECTION of the constraints' GT-neighbor sets;
accept iff single unclaimed row; reject+count collisions.

**Result: 970 → 1073 (pass1) → 1113 (pass2), then collisions 9.1% > 2% AND
growth < 1500 → STOP (both spec halt conditions).** Only +143 slots.

## THE STALL IS SOUND, NOT A BUG (proven)
- **Soundness:** re-running alloc7 S-fit on the DENSE map → **671/743 = 90.3%**
  (up from 558/623), with 117 fitted rails (up from 101). If densify were
  injecting garbage, adding those slots would inject wrong GT-edge tests and the
  score would DROP. It rose. What densify adds is correct.
- **(Trap avoided:** validating new slots against `P_base` XZ is INVALID — the
  unmapped slots are exactly the ones whose coord decode failed, so `P_base[s]`
  is garbage there; 45% fall outside the model bbox. Do not "score" densify
  against P_base.)

## WHY IT STALLS (the binding limit, fully quantified)
The 970 anchors are a **scattered seed**. In emission order: 289 contiguous
unmapped runs, mean length **6.9**, max **158**; 74 runs ≥5 slots cover 1611
slots; 1389 slots sit in runs ≥10. Constraint census over the 1862 unmapped
slots (dense map): **1477 have ZERO constraints**, 316 have exactly ONE (can
never single-row-resolve), only 69 have ≥2 — and of those only 12 give a single
unclaimed row (53 give EMPTY intersection, because emission-adjacency is only 40%
reliable so the two neighbor-sets usually don't share the true row).

**Propagation can only nibble run EDGES (~1 slot/pass/end, 40% reliable); it can
never fill run interiors.** The answer key is capped at the coord decode's
XZ-anchorable fraction (~32%); topology cannot bootstrap across a region where
NO slot is decoded. Densification was trying to route around the coord/Y wall
(Mystery E) and is blocked by that same wall. **This approach is a dead end.**

## THE REFRAME (why we are NOT stuck)
Step 2 — reading the n0/d encoding — **does not need a dense map.** The
alloc11 chain reconstruction on the current map already isolates **57 exact INIT
sites** `(gi, rail, n0, first_ref r)`. 57 labeled triples is a workable
correlation set. Deliverables emitted for the Fable session:
- **`python/init_sites.txt`** — the 57 INIT sites with delim, ops, refs, and raw
  `.00t` bytes `[pos−6 : pos+18]` around each group start.
- **`python/strip_table.npy`** — 57 strips, cols `(gi_start, gi_end, rail_id, n0,
  d, len, resolved_frac)`; 10 strips have ≥3 resolved allocs, 113 resolved total.

### Detective leads already visible in `init_sites.txt` (for Fable)
1. **n0 is NOT a literal 2-byte value** in any window (0/57 for n0, 0/57 for
   S=n0+r, both endian). Confirms RESUME-07-08. n0 is encoded indirectly or is
   pure derived state.
2. **n0 RECURS across strips** (777, 1327, 1205, 1262, 1046, …) and always sits
   a small offset below its first_ref r (n0 = S−r, S per-rail constant). Strong
   hint that **n0 is DERIVED, not stored** — there may be nothing to "read" for
   n0; the only per-strip freedoms are S (= n0+r, recurs per rail) and d (±1).
   Test hypothesis (b) from RESUME-07-08: n0 = a boundary in the tiling of
   already-allocated blocks (fold = end of previous strip's block ±1).
3. **INIT first-op** is 38× `60:17` / 15× `40:17`; the `40:17` share is elevated
   at INIT (26%) vs CONT (16%) but noisy. The richer signal is the **SECOND op**
   (`0x4X:YY` / `0x2X:YY` announce-class) that varies per site — census it
   against S and d on the strip_table.

## NEXT STEP (FABLE): read n0/d from the 57 INIT sites — `init_sites.txt`
Do NOT try to densify the map further (proven dead). Instead:
1. Test lead #2 first: on `strip_table.npy`, check whether each strip's n0 is
   fully predicted by (S of the rail, r, d) — i.e., n0 = S − r with S = the
   round-robin rail's constant. If yes, "INIT encoding" collapses to encoding S
   and d, not n0. Verify d = −sign(ref-rail direction) per the strip table.
2. Correlate the SECOND-op byte (and the `40:17` vs `60:17` first-op flag)
   against (strip-start? d flip? S value) across the 57 sites.
3. If a rule emerges, fold it into the GT-free round-robin strip machine
   (RESUME-07-08 "THEN" section) and score via the validation ladder there.

## DEAD THIS SESSION (do not retry)
- **Pure-propagation answer-key densification** (densify.py, spec NEXT STEP 1):
  stalls at 1113; blocked by coord-decode sparsity, not fixable by better
  intersection logic. Widening emission adjacency to s±2/±3 only adds noise
  (offset-1 is 40%, ≤3 is ~47%).
- **Scoring densify output against P_base positions** — invalid (P_base garbage
  on exactly the unmapped slots).
