# RESUME 2026-07-10 — Fable session: the 57 "INIT sites" are NOT strip starts; n0 confirmed fold-shaped (derived); next door = op-channel run structure

**Read RESUME-2026-07-09.md first** (densify dead, 57 INIT sites, n0-derived
lead). This doc records the Fable session that executed its NEXT STEP (read
n0/d from the 57 sites). Net result: **the read is impossible at those sites
because they are not strip starts** — but n0's structural nature got pinned,
and a concrete GT-free strip-segmentation lead emerged. Scripts:
`python/init_read.py` … `init_read8.py` (+ `full_flags.npy`).

## FINDINGS (in evidence order)

1. **Pre-delim value == r is CIRCULAR.** bytes[-6:-4] BE == r at 39/57; the
   misses are just the other ref encodings (`00`+low-byte splice keeping prev
   high byte, 9/10; `20:11`-sep low-byte-only form; `07 01` prefix with full r
   shifted +2). That value IS the preceding ref group's last ref — which is
   exactly how alloc11 *defined* r. Confirms ref-encoding forms; reads nothing.
2. **The 57-site list is CONTAMINATED: 26/57 n0 slots were already referenced
   BEFORE their gi** (init_read4). These are first-resolved-slots of heuristic
   rail FRAGMENTS (JUMP=6/RETIRE=80 clusterer), not strip starts. Recurring
   (n0,r) with byte-IDENTICAL 24-byte windows at different gi (5209/5402,
   5221/5414, 5252/5445, 5258/5451, 5295/5488) confirm ops carry PHASE state,
   not strip identity (07-07 KEY FACT re-verified at these sites).
3. **POSITIVE — n0 is a serpentine fold/column boundary.** n0 sits at
   emission-adjacency breaks at 10–18× base rate (isolated slot 15.8% vs 0.9%
   base; column-end 10.5% vs 1.1%; any-break 42% vs ~4%) — init_read2 T1 on
   the 970-anchor map. Hypothesis (b) "n0 = block/column boundary, DERIVED not
   stored" is structurally supported.
4. **NEGATIVE — folds are NOT FULL-announced in the coord stream** (init_read3:
   14% vs 12.8% base; `full_flags.npy` = per-slot FULL/axis flags from the
   v5-P2 decode). Column jumps are delta-encoded; record TYPE cannot mark folds.
5. **NEGATIVE — n0 has no law vs fragment-rail extents** (init_read5: n0−lo/hi/
   r_end scatter; rails truncated by the clusterer).
6. **NEGATIVE — e002/e004 are NOT rail birth/death markers** (init_read7:
   38%/35% within 6 groups vs ~42% control). BUT their counts (216 vs 225)
   remain push/pop-shaped; the failure may be the rail clusterer's noise, not
   the hypothesis. Un-retire this only with a better strip model.
7. **d = −sign(ref-rail direction): 13/14 on clean rails** (init_read2 T3; the
   one violation rid=434 needs the d-sign convention pinned — the strip_table's
   d convention vs RESUME-07-08's "n(t+1)=n(t)−d" was never locked down).

## THE DOOR (new lead — detective work, Fable)

**Ref-group first-op arg 6-bit channels form INTERLEAVED arithmetic runs**
(init_read8, g100–190): lead-0x42 groups walk channels 1,7,13,19(,25) — the
07-07 "+6 per phase" chain — interleaved with a lead-0x41 chain (41,53,59,60)
and others (42:37=13, 44:bf=47, 43:e7=57). Channel is constant within a run
(~3–4 ref/idless pairs), runs alternate between chains. If run structure =
round-robin SCHEDULE (each run = one strip's turn, chain = sweep counter),
then **schedule-change events = strip push/pop = TRUE strip segmentation,
GT-free** — the thing the 57-site list failed to be.

## NEXT STEP (FABLE): decode the channel schedule

1. Extract per-group `(gi, delim, refs?, lead_hi, lead_lo, channel=arg>>2,
   lo2=arg&3)` for the whole stream (init_read8 skeleton; lo2==3 at 96%).
2. Run-length encode by (lead, channel); map run transitions. Test: does the
   sequence of runs form a repeating cycle of K distinct (lead,channel) chains
   with each chain advancing +6 (or +const) per revisit? K should match the
   ~13 concurrent strips.
3. Where the cycle CHANGES membership (a chain appears/disappears) — census
   against: e002/e004 gi's, e0ff/e1xx gi's, rail births ≥3-event, the 31
   clean INIT sites (marked * in init_read5 output). If push/pop events land
   on a delим class, that class + channel gives the strip machine its
   push/pop records and turn order — then n0 needs NO encoding: fold = next
   unallocated column boundary when the strip enters, d from rail direction
   (finding 7).
4. Validation: #push ≈ #pop ≈ 216–225 (e002/e004 counts); round-robin size
   trace should stay ~13; replay allocation with the schedule + n=S−r counter
   and re-score alloc7-style (>90% = the machine is real).

## PROBE RESULT (init_read9.py, same night — kill criterion hit)
Naive +6/+12 chain-linking of ref-group channel runs: only 48% of runs link
(needed ≥60%); 809 chains vs ~220 expected; chain starts/ends at e002/e004 =
control rate (28% vs 29%). NOT dead — concurrency trace (8–13 alive) matches
the ~13-strip round-robin, and 184 chains reach ≥3 turns — but the naive
linker is wrong (mod-64 wrap + skipped turns + lead-byte changes need a real
model). Next session: fit the schedule properly (start from long chains,
learn the step alphabet, THEN re-test push/pop). STOPPED here per budget.

## PROBE 2 (init_read10.py) — CHANNEL IS A GLOBAL PHASE CLOCK, NOT STRIP DATA
Same-rail consecutive turns: channel steps are ALL multiples of ±6 (mod 64):
0,±6,±12,±18,±24,… (top-12 census exclusively these). So channel = the 07-07
global sweep-phase counter (+6 per phase, mod 64) — a strip's turn shows
+6×(phases elapsed). It CANNOT mark push/pop per strip. Kill criterion hit
(top-3 = 28% < 50%). THE DOOR section above is therefore CLOSED as a
segmentation source; channel remains useful only as a phase clock (may help
align sweeps). Strip segmentation must come from elsewhere (delim families
e0ff/e1xx unexamined; or the idless-group op bit-flips 60/40:17, 2X/4X,
20/40:1b).

## DO NOT RETRY (this session's dead ends)
- Reading n0/S as literal or spliced values in the ±6/±18-byte INIT windows —
  the only value there is the previous ref (finding 1).
- FULL-record-based fold detection (finding 4).
- Densify (07-09), rail-extent laws (finding 5), e002/e004-vs-rail-events at
  current clusterer quality (finding 6).

## SIDE NOTE (user Q, 07-05 session): intercepts_compare.html shows 26.7%
That page embeds a Jul-4 snapshot (commit 65fb6e4). The 98.3% of ce22479 was
CANDIDATE recall (2-of-3 lenient fingerprint), not a sequential decode; the
strict decoder is at ~27%@1m — the known Mystery-E ceiling. Scattered red pts
= misframed/mis-spliced records (45% of unmapped slots out-of-bbox). Topology
sessions since Jul 4 don't feed this viewer. Nothing regressed.
