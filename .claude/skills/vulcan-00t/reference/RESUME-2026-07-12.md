# RESUME 2026-07-12 — S went GT-free & first faces render; then hit a REAL plateau (coords 52% / faces 9.2%). Four levers ruled dead.

**Read this FIRST, then `STRIP_SCHEDULE.md` (root) and `Z_RECONSTRUCTION_HUNT.md`.**
Supersedes the earlier 07-12 draft (which still listed "GT-free S" as the open
task — that is now DONE; the wall moved).

## HONEST HEADLINE — a flat night on the scoreboard, real night on the map

The decode numbers did NOT move tonight: **coords 52.1%→52.2% GT-free @0.25m,
faces ~9% coverage.** What moved is *understanding* — S is now derived, faces
render, and four plausible growth levers are eliminated with evidence. In a
9-month RE, ruling levers out IS the work; but do not dress it up as a metric
gain. (User challenged an earlier "85%" oversell — the honest number is **9.2%**
distinct-GT-face coverage; 85.6% was precision-on-the-scorable-subset only.)

## WHAT THIS SESSION ADDED (2026-07-11→12)

1. **S is now GT-free.** `S = 2×fold apex` (a strip = the ZIP of one serpentine
   fold; side-bit = which of a rail's two folds it zips). Fable column partition
   (`columns_v1.pkl`) + mirror-geometry voting → `side_rule.pkl` / `side_rule_ext.pkl`
   ({rid:(side,S)}). Side is **90.5% @ apex-margin≥3**, noisy below (teacher key
   itself proven ~coin below margin 2 — both fold choices locally GT-consistent).
2. **First GT-free faces render** (`side4_emit.py` → `decoded_faces_ext.json`),
   with an honest 4-class viewer (`side13_viewer.py` → `js/intercepts_faces.html`):
   class 3 GREEN = correct topology + all verts placed (329); class 2 ORANGE =
   correct topology but ≥1 vert NOT decoded — **the spikes** (238); class 1 RED =
   wrong topology (98). **The spikes are a COORD problem, not a faces problem.**
3. **Honest accounting** (`side6_accounting.py`): 525 distinct GT faces / 5724 =
   **9.2%**; 1078 tris emitted = 18.8%; 85.6% is the scorable-subset precision only.

## FOUR LEVERS RULED DEAD TONIGHT (do NOT retry — evidence in commits)

- **Strip stitching across rail seams** (`side12_stitchquad.py`): +1 face. Chains
  too short (435/670 rails ≤2 turns; `side9_ceiling.py` → per-quad ceiling ~18-37%,
  rail fragmentation is the cap). I over-promised this; it is dead.
- **S-extension 239→311 rails** (`side8_extendS.py`): marginal, no coverage move.
- **Coord residual filter §7.2** (`decode_v12_z.py`, arg `DROP_TH`): **no-op** —
  52.1% unchanged; the +5 verts at NPASS=4 were the extra pass, not the filter.
- **Faces-for-Z joint bootstrap** (`side14_facez_oracle.py`, Z_RECON §6): **REFUTED.**
  Using decoded-face topology for the Z-surface neighbours is *worse* than
  XY-nearest — median |err| 2.07 m vs 0.031 m; within-half-window 1% vs 82%; face
  strictly better on only 1/137 yellows. **Coords and faces are far LESS coupled
  than the docs assumed** — the joint solve is not the coord lever.

## WHERE THE TWO WALLS ACTUALLY ARE NOW

- **Coord 52% ceiling = SURFACE-BOOTSTRAP quality**, not neighbour selection and
  not missing topology (Z_RECON §6b). The Z high-byte is a local surface prediction
  through the decoder's OWN already-decoded XY-nearest neighbours; feeding it its
  own 52%-correct output as priors self-limits at ~52%. `plane_ref()` (median of
  plane-interps over 8 XY-nearest) is a **stand-in** for the encoder's true Z
  predictor — which is still UNKNOWN. Cracking that exact predictor is the
  higher-leverage move; it gates everything visual.
- **Faces 9.2% ceiling = rail fragmentation.** The per-quad machine is precise
  where a rail has ≥3 turns, but most rails are ≤2 turns and emit nothing. Growth
  needs S onto short rails (alloc turn-assignment for the ~593 uncovered refs), not
  a better per-quad rule.

## NEXT STEPS (spec-shaped)

### Track A — exact coord Z predictor (FABLE detective, highest leverage)
Hypothesis space is OPEN, so this is Fable, not Opus.
- **Goal:** replace `plane_ref()` median-of-XY-nearest-planes with the encoder's
  actual per-vertex Z reconstruction rule, so pass-2 exceeds 52% *without* using
  its own noisy output as the prior.
- **Mine:** the k0/carry machinery + `full_flags.npy` + v11 fold signatures for a
  DETERMINISTIC neighbour set (e.g. the two serpentine-adjacent already-decoded
  verts in emission order, not spatial XY-nearest) that the encoder would have had
  in hand at emit time. The oracle harness pattern is `side14_facez_oracle.py`
  (swap the candidate neighbour set, compare median|err| and half-window% vs the
  XY-nearest baseline on the *yellow* set = XY-ok/Z-wrong verts).
- **Validation ladder:** candidate predictor beats XY-nearest on yellow median|err|
  → drop it into `decode_v11_z.py` neighbour selection → GT-free % on intercepts
  must exceed 52.2% → SYLVANIA/OB34 cold must NOT regress (no fingerprint).

### Track B — alloc turn-assignment for uncovered refs (OPUS-executable once specced)
- 593 refs are unassigned to turns; assigning them extends S onto short rails and
  lifts the 9.2% coverage. This is spec-shaped enough for Opus IF Track A's column
  quality holds. Blocked-behind: coverage is capped by rail fragmentation, so
  measure the ceiling first (`side9_ceiling.py`) before investing.

### Then (unchanged backlog): fan dn sign + A/B phase from zigzag state → full-stream replay → port to `js/vulcan00TParser.js`.

## FILES ADDED/CHANGED THIS SESSION
- `python/side4_emit.py` (GT-free tri emit, geometric-diagonal quad split),
  `side6_accounting.py` (honest coverage), `side8_extendS.py`, `side9_ceiling.py`,
  `side12_stitchquad.py` (stitch — DEAD), `side13_viewer.py` (v2 4-class viewer),
  `side14_facez_oracle.py` (faces-for-Z — REFUTED), `decode_v12_z.py` (residual
  filter — no-op).
- `columns_v1.pkl`, `side_rule.pkl`, `side_rule_ext.pkl`, `decoded_faces_ext.json`,
  `P_v11_intercepts.npy`, `GT_intercepts.npy`.
- Docs: `Z_RECONSTRUCTION_HUNT.md` §6b (both pass-2 levers dead), `STRIP_SCHEDULE.md`
  (§COVERAGE CEILING).
- Still dead/contaminated: `rails.pkl`, `ma_beam*`, `ma_replay`, `ma_slotanchor`,
  JUMP=6 anything, strip-stitcher.

## STATUS SNAPSHOT (honest)
- Container/sections/header ~100%. Coord VALUE codec **52% GT-free @0.25m** (dev),
  43.7% SYLVANIA cold — ceiling = surface-bootstrap self-reference.
- Faces: framing + op semantics + ref indices + rail schedule + **S now GT-free** +
  first render. Honest coverage **9.2%** distinct GT faces. Ceiling = rail
  fragmentation.
- **Both subsystems at current-mechanism ceilings; obvious tweaks exhausted.**
  The two live fronts are both harder detective work: exact Z predictor (Track A,
  Fable) and alloc turn-assignment (Track B). Joint bootstrap is REFUTED as a lever.
