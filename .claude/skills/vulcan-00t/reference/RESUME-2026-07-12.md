# RESUME 2026-07-12 — Rails are REAL (interleaved ±1 walks); strip machine at 82.3% teacher-forced; next = GT-free S

**Read this FIRST, then `STRIP_SCHEDULE.md` (repo root).** Supersedes
RESUME-2026-07-11's "sparse per-fold anchors" framing (the anchors ARE dense;
the −95..−160 steps are round-robin switches between ~13 concurrent strips).
`refs_v2.pkl` itself stands unchanged — only its interpretation moved.

## WHAT THIS SESSION PROVED (2026-07-11 Fable, scripts strip1..strip10)

1. Real refs = **~10–13 concurrent ±1 rails** (one per live strip, spacing ≈
   column pitch ~70), visited round-robin; turn = ref group + following
   idless group(s), modal 1+1 = one quad per turn.
2. **Strict ±1 value-continuity clustering** (`strip10_strict.py` →
   `rails_v3.pkl`): 678 rails, steps exclusively {±1, 0}; concurrency 13.
3. Strip machine on strict rails, teacher-forced fitS [GT votes, stated]:
   **quad rate 82.3%** (297/361; was 56.3% pre-fix), |dr|=1 subclass 84%,
   purity-1.0 rails 90%, several rails 100%. **Mirror n=S−r edge rate 92.7%.**
4. **Fans**: mirror can't advance (0%); counter-step n=last_n+dn gets 50% —
   dn sign rule open.
5. REFUTED: free-running per-rail alloc counter (9.4% — must mirror-resync
   each turn); frontier-relative refs (global alloc counter); 1:1 group↔DXF
   face order (~2%).

## NEXT STEP — GT-free S (Fable detective; the ONE remaining wall)

S (per strip, per fold) is the only teacher-forced input left in the face
machine. S = r0 + n0 (the strip's two column feet). Attack in order:

1. **Lo-channel retest on clean rails.** `00 pp<0x80` structural events
   (1425) were never tested against TRUE rails. Re-tokenize with
   `ma_refdecode.py`'s loop, capturing per-group pp values (refs_v2.pkl does
   not store them), then correlate pp against: rail id at that gi, S−r,
   turn length, rail birth (first turn), fold events (dr sign change).
   Count-shaped, GT-free. If pp encodes n0/S offsets at rail birth → done.
2. **Coord-side fold boundaries.** n0 = column boundary (RESUME-07-10
   finding 3, 10–18× adjacency breaks). Column boundaries are visible
   GT-free in the coord decode (v11 fold signatures / k0 sites). At rail
   birth, S := (next column boundary) + r0. Test against fitS values
   (fitS is GT-derived, use as answer key for MECHANISM validation only).
3. **e002/e004 vs rail_v3 births/deaths.** Counts (216/229) are push/pop
   shaped; the old test used contaminated rails (init_read7 negative).
   Rails_v3 births = 678 ≫ 216 (rails fragment at coverage gaps), so test
   on long rails only, and test WHICH strip a push initializes (next e003
   turn after e002?).

Then (Opus, spec-shaped): fan dn + A/B phase from zigzag state; alloc turn
assignment for the 593 uncovered refs; full-stream replay; validation ladder
teacher-forced ≥85% → GT-free scorer (parallelogram + edge-band + valence)
→ GT face score [GT-free category] → SYLVANIA cold.

## FILES
- `STRIP_SCHEDULE.md` (root) — full evidence + model.
- `python/strip4_rails.py` → `rails_v2.pkl` (greedy, superseded);
  `strip10_strict.py` → `rails_v3.pkl` (STRICT — use this).
- `strip5_replay.py` (quad census 73.9→82.3 via strict), `strip6_machine.py`
  (free-run refutation), `strip7_align.py` (DXF-order refutation),
  `strip9_loss.py` (loss decomposition — the decisive |dr| table).
- Unchanged: `refs_v2.pkl`, `map11.pkl`, `faces_gt.npy`.
- Still contaminated/dead: `rails.pkl`, `ma_beam*`, `ma_replay`,
  `ma_slotanchor`, JUMP=6 anything.

## STATUS SNAPSHOT (honest)
- Container/sections/header ~100%; coord VALUE codec ~52% GT-free @0.25m
  (dev), 43.7% SYLVANIA cold.
- Faces: framing + op semantics + ref indices + **rail schedule structure**
  solved; machine 82.3% teacher-forced on strict rails. Remaining GT-free
  gaps: S per strip (the wall), fan dn, phase, alloc turn assignment.
